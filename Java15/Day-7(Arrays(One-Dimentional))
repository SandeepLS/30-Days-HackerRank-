/*
An array is an container object, that holds a fixed number of values of a single type.
All arrays are dynamically allocated, and stored in consecutive memory location.

One-dimentional array:-
                        type var-name[];
                        OR
                        type[] var-name;

Instantiating an Array in Java:- var-name = new type [size];
Ex:-
int intArray[];    //declaring array
intArray = new int[20];  // allocating memory to array
OR 
int[] intArray = new int[20]; // combining both statements in one

Reffer:-https://www.geeksforgeeks.org/arrays-in-java/?ref=lbp
*/

import java.io.*;
import java.math.*;
import java.security.*;
import java.text.*;
import java.util.*;
import java.util.concurrent.*;
import java.util.function.*;
import java.util.regex.*;
import java.util.stream.*;
import static java.util.stream.Collectors.joining;
import static java.util.stream.Collectors.toList;

public class Solution {
    public static void main(String[] args) throws IOException {
        Scanner scan = new Scanner(System.in);
        
        int size = scan.nextInt(); //4  //Declearning array
        int a[]= new int[size];         //Creating array
        
        for(int i=0;i<size;i++)         //Initializing
        {
            a[i]=scan.nextInt(); //1234
        }
        for(int i=size-1;i>=0;i--)
        {
            System.out.print(a[i]+ " ");//4321
        }
        
        scan.close();
    }
}
